<INSTRUCTIONS>

## ШВИДКІ КОМАНДИ

| Команда | Значення |
|---|---|
| `+` | Рухатись по бeклогу (`backlog.md`): виконувати наступну невиконану задачу, розвивати та покращувати проєкт, будувати цільовий продукт. Читай бeклог, знайди першу незавершену Phase/задачу, виконай її. |

---

## WORKFLOW — ОБОВ'ЯЗКОВИЙ ДЛЯ КОЖНОГО ЗАПИТУ

### Крок 1: Вхід у контекст (ПЕРЕД кожним запитом)

**Перед виконанням БУДЬ-ЯКОЇ задачі** агент ЗОБОВ'ЯЗАНИЙ прочитати ВСЮ документацію повністю:

```
ОБОВ'ЯЗКОВО ПРОЧИТАТИ (в цьому порядку):
1. docs/PROJECT_STATUS.md    — Поточний стан проєкту, що працює, що ні
2. docs/CONTINUATION_GUIDE.md — Як продовжити роботу, типові помилки, SSH доступ
3. docs/SESSION_LOG.md        — Хронологія всіх сесій та прийнятих рішень
4. docs/ARCHITECTURE.md       — Технічна архітектура (Docker, порти, мережі, шифрування)
5. docs/TRANSFER_SYSTEM.md    — Система трансферу персонажів та login bot
6. docs/COMMANDS_REFERENCE.md — Довідник всіх команд (SSH, DB, Docker, Bot, RA)
7. backlog.md                 — Детальний бэклог всіх виконаних фаз
```

**НЕ ПОЧИНАЙ РОБОТУ**, поки не прочитав кожен файл цілком. Це не рекомендація — це вимога.

### Крок 2: Виконання задачі

Після повного прочитання документації — виконуй задачу користувача.

### Крок 3: Оновлення документації (ПІСЛЯ кожної зміни)

**Після КОЖНОЇ зміни** (код, конфігурація, інфраструктура, фікс, нова фіча) агент ЗОБОВ'ЯЗАНИЙ оновити відповідні документи:

| Що змінилось | Які файли оновити |
|---|---|
| Стан сервера, акаунти, GUI, нова фіча | `docs/PROJECT_STATUS.md` |
| Архітектура, порти, Docker, мережі | `docs/ARCHITECTURE.md` |
| Трансфер, login bot, SQL міграції | `docs/TRANSFER_SYSTEM.md` |
| Нові команди, зміна існуючих | `docs/COMMANDS_REFERENCE.md` |
| Будь-яка змістовна дія в сесії | `docs/SESSION_LOG.md` (додати запис) |
| Нові знання для наступного агента | `docs/CONTINUATION_GUIDE.md` |
| Завершена фаза роботи | `backlog.md` (додати фазу) |

**Правила оновлення:**
- Оновлюй ТІЛЬКИ ті файли, яких стосується зміна
- НЕ видаляй існуючу інформацію — додавай нову
- Хронологічні записи (SESSION_LOG) додавай в кінець
- Якщо створюєш новий файл у проєкті — додай його в PROJECT_STATUS.md (секція "Файлова структура")

### Підсумок воркфлоу

```
┌─────────────────────────────────────┐
│  1. ЧИТАЙ всю docs/ + backlog.md   │
│     (повністю, кожен файл)          │
├─────────────────────────────────────┤
│  2. ВИКОНУЙ задачу користувача     │
├─────────────────────────────────────┤
│  3. ОНОВЛЮЙ документацію           │
│     (кожна зміна = оновлення docs)  │
└─────────────────────────────────────┘
```

**Мова спілкування: УКРАЇНСЬКА.** Код та команди — англійською.

---

## Оригінальне ТЗ

Кінцевий продукт:
1) На ssh workspace функціонуючі Docker контейнери (Classic, TBC, WotLK) — **✅ ВИКОНАНО**
2) Конфіг файли та map data — **✅ ВИКОНАНО**
3) Зручні команди для БД — **✅ ВИКОНАНО** (docs/COMMANDS_REFERENCE.md)
4) Зручні команди для update — **✅ ВИКОНАНО** (systemd timers)
5) Zero artifacts on host — **✅ ВИКОНАНО**
6) Екстракція карт — **✅ ВИКОНАНО**
7) Архітектура: aarch64 (ARM64)

## Поточна Задача

**Виправити TBC→WotLK креш персонажа** — побітове тестування числових полів character row з використанням login bot (`transfer/wow_login_test.py`). Деталі в `docs/TRANSFER_SYSTEM.md` розділ 6.

## Ключові Координати

- **SSH:** `ssh -o ControlPath=/tmp/ssh-ws workspace` (Yubikey ED25519-SK)
- **Remote IP:** 64.181.205.211
- **Docker Hub:** semorgana
- **DB Password:** $DB_PASSWORD
- **WotLK account SAMUEL:** ID=12, password TEST123 (SRP6), GUID=1802
- **WotLK account ADMIN:** ID=13, password TEST123 (SRP6), gm3, RA port 3443
- **Порти:** Classic 8085/3724/3306 | TBC 8086/3725/3307 | WotLK 8087/3726/3308

</INSTRUCTIONS>