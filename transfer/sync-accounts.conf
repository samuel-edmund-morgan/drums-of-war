# CMaNGOS Daily Sync — Account List
# Format: USERNAME:PASSWORD (one per line, case-insensitive)
# Lines starting with # are comments
#
# All characters on each account will be synced:
#   Classic → TBC → WotLK
# Hash-based conflict detection per character.
# Accounts are auto-created on target if missing.
# Password is used for login bot verification after transfer.

samuel:samuel
